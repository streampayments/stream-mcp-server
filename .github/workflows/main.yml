name: CI (Build -> Lint -> Push)

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

on:
  push:
    branches:
      - main

jobs:
  build-lint-push-image:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Checkout shared actions
        uses: actions/checkout@v4
        with:
          repository: streampayments/stream-actions
          token: ${{ secrets.STREAM_GH_TOKEN }}
          path: .github/stream-actions

      - name: Setup Python Env
        uses: ./.github/stream-actions/.github/actions/setup-python
        with:
          docker-cache-read-only: true

      - name: Setup Env Vars
        # This repo config: create .env from .env.example if available.
        run: |
          if [ -f .env.example ]; then
            cp .env.example .env
          else
            touch .env
          fi

      - name: Run CI
        # Shared/internal action (specific to Stream org tooling)
        uses: ./.github/stream-actions/.github/actions/ci-python
        with:
          should-lint: true
          # This repo currently has no tests
          should-setup-test-containers: false
          should-test: false
          should-check-coverage: false

      - name: Push image to ECR
        uses: ./.github/stream-actions/.github/actions/push-image-to-ecr
        with:
          ecr-access-key-id: ${{ secrets.AWS_ECR_ACCESS_KEY }}
          ecr-secret-access-key: ${{ secrets.AWS_ECR_SECRET_KEY }}
          ecr-region: ${{ secrets.AWS_ECR_REGION }}

      - name: Update service version
        uses: ./.github/stream-actions/.github/actions/update-service-version
        with:
          gh-token: ${{ secrets.STREAM_GH_TOKEN }}
          service-name: ${{ github.event.repository.name }}
          service-version: ${{ github.sha }}
          environment: dev
